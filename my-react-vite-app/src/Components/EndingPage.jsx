// import { useEffect, useState } from "react";

export const EndingPage = () => {
    //     const [currentScene, setCurrentScene] = useState(null);

    // // Fetch the story.json data
    // useEffect(() => {
    //     const fetchStoryData = async () => {
    //         try {
    //             const res = await fetch("/story.json"); // Ensure the file is in the public folder
    //             const data = await res.json();
    //             const endPage = data.stories.find(
    //                 (story) => story.id === "ending-page"
    //             );
    //             setCurrentScene(endPage);
    //         } catch (error) {
    //             console.error("Error fetching story data:", error);
    //         }
    //     };

    //     fetchStoryData();
    // }, []);

    // // Wait for the data to load
    // if (!currentScene) {
    //     return <p>Loading...</p>;
    // }

    return <div>ENDING PAGE</div>;
};
